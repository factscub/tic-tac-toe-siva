import React from 'react'
import { FormContainer } from '../../Components/FormContainer/FormContainer'

export const NewGamePage = (props = {}) => {
    
    return (
            <FormContainer {...props} />
    )
}
